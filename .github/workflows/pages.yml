name: GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Repository laden
        uses: actions/checkout@v4

      - name: GitHub Pages konfigurieren
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Website vorbereiten
        run: |
          mkdir _site
          cp index.html partner.html styles.css script.js cinematic.js partner.js _site/
          cp -R assets _site/assets
          cp -R vendor _site/vendor
          touch _site/.nojekyll

      - name: Website-Paket hochladen
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: GitHub Page veröffentlichen
        id: deployment
        uses: actions/deploy-pages@v4
